# Yarn adversary

This repository contains an Adversary Labs adversary. Keep checks deterministic and evidence-backed. Parse repository files without executing target code. Add focused vulnerable and clean fixtures for every rule. Run `npm test`, `doomer validate .`, and `doomer pack --check .` before release.
